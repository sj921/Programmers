SELECT
    CATEGORY,
    SUM(SALES) AS TOTAL_SALES
FROM
    BOOK
JOIN
    BOOK_SALES
USING
    (BOOK_ID)
WHERE
    TO_CHAR(SALES_DATE, 'YYYYMM') LIKE '202201%'
GROUP BY
    CATEGORY
ORDER BY
    CATEGORY;